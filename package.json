{
  "name": "exit-on-dirty-git",
  "version": "1.0.1",
  "description": "Exit with status code (1) if something is uncommited in current tree",
  "main": "bin/exit-on-dirty-git.js",
  "bin": {
    "exit-on-dirty-git": "bin/exit-on-dirty-git.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["git", "dirty", "exit"],
  "author": "sudodoki <smd.deluzion@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "dirty-git": "^3.1.1"
  }
}
